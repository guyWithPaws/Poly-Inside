name: shared
publish_to: none

environment:
  sdk: ^3.5.2

# Add regular dependencies here.
dependencies:
  grpc: ^4.0.4
  protobuf: ^4.0.0

dev_dependencies:
  build_runner: ^2.4.12
  lints: ^4.0.0
  test: ^1.24.0
